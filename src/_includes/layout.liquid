<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="/bundle.css">
    <title>{{ page_title | default: "gtnoblg"  }}</title>
  </head>
  <body>
    <header role="banner" class="{{ header_class }}">
      <h1>{{ header_markup }}</h1>
    </header>
    <main>
      {{ content }}
    </main>
  </body>
</html>
